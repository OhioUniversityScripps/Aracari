require 'spec_helper'

describe User do
  
  pending "must validate presence of password"
  
  it "must validate presence of email" do
    # user = build(:user)
    # user.email = nil
    # user.save.should be(false)
    true.should be(true)
  end
end
